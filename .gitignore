node_modules/
.next/
.env
.env.local
.data/
*.tsbuildinfo
next-env.d.ts
.vercel
.env*
.claude/worktrees/

# Agent tooling: editor/assistant configuration, not project source. Kept out of
# the repo so the published tree is the product and nothing else. All of it is
# local-only and regenerable — the skills trees restore with
# `npx skills experimental_install`.
.claude/
.agents/
.impeccable/
skills-lock.json

# Derived knowledge graph (graphify): ~2.4MB of generated JSON/HTML rebuilt from
# the source tree on demand. The graph is an analysis artifact, not source.
graphify-out/
